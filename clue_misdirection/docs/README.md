# Rendered Notebooks — CCC Clue Misdirection

These HTML files are rendered versions of the project notebooks with full outputs
(figures, tables, metrics). Open any file in a browser to view.

| Notebook | Description |
|----------|-------------|
| 00_model_comparison | Baseline model comparison across embedding conditions |
| 01_data_cleaning | Filter clues, validate WordNet coverage, export clean dataset |
| 02_embedding_generation | Generate CALE embeddings for definitions, answers, and surfaces |
| 03_feature_engineering | Construct feature table (cosine similarities, WordNet features, etc.) |

More notebooks will be added as the pipeline progresses. See `PLAN.md` for
the full 12-step pipeline plan.
